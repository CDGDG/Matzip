version: "3"
services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - .:/src
      - ./nginx:/etc/nginx/conf.d
    depends_on:
      - web

  web:
    build: .
    command: bash -c "
      python3 ./manage.py collectstatic --no-input &&
      python3 ./manage.py makemigrations &&
      python3 ./manage.py migrate &&
      gunicorn ./config.wsgi:application -b 0:80"
    volumes:
      - .:/src
      - ./config/gunicorn:/etc/systemd/system